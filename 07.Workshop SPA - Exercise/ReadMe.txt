Open the terminal with CRTL + J and install the following libraries with the command:

npm install handlebars sammy jquery lite-server

Open the app with live server or you can directly use lite-server with the following command:

npx lite-server